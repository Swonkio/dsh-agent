# Memory index

One line per topic; detail in topics/<slug>.md. Maintained by memory_save.

- Pi model serving: llama-server serves bonsai-27b on 127.0.0.1:8080 with 8192 context, started manually (no systemd)
- Cron defaults: dsh-cron jobs default to provider zai model glm-4.5-air; the runner fires via user crontab every minute
- Agent name: The user calls this agent "Neo" — respond to that name.
